name: receipt-pwa

services:
  app:
    build: .
    image: receipt-pwa-app:latest
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    tty: true

volumes:
  node_modules:
